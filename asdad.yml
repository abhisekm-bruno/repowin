info:
  name: asdad
  type: http
  seq: 1

http:
  method: GET
  url: firefox.com
  auth:
    type: awsv4

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("user_name", "Luke_1")

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
